<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit product</title>
</head>

<body>
    <h1>Edit product</h1>
    <form action="/products/<%= product._id %>?_method=PUT" method="POST">
        <label for="">Product Name</label>
        <input type="text" name="name" placeholder="product name" value="<%= product.name %>">
        <label for="">Price</label>
        <input type="number" name="price" placeholder="$"  value="<%= product.price %>">
        <label for="">category</label>
        <select name="category" id="">
            <option <%= product.category == 'fruit' ? 'selected' : ''%> value="fruit">fruit</option>
            <option <%= product.category == 'vegetable' ? 'selected' : ''%> value="vegetable">vegetable</option>
            <option <%= product.category == 'dairy' ? 'selected' : ''%> value="dairy">dairy</option>
        </select>
        <button>Submit</button>
    </form>
</body>

</html>